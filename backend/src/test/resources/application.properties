# Test configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/big_data_energy
spring.datasource.username=bigdataenergy
spring.datasource.password=bigdataenergy
spring.jpa.hibernate.ddl-auto=update

# Google OAuth test configuration (placeholder values for integration tests)
google.oauth.client-id=test-client-id
google.oauth.client-secret=test-client-secret
google.oauth.redirect-uri=http://localhost/test-callback

# Google Maps API (placeholder)
google.api.key=test-api-key
